package day14_string;

public class StringMethodos {
    public static void main(String[] args) {

        //String str1 = "Loop"; // By LITERALS
        //String str2 = "Loop"; // By LITERALS
        //String str3 = new String("Loop"); // By "new" keyword

        // 1. "".equals("");
        // System.out.println(str1.equals(str2);


        // 2. "".equalsIgnoreCase(""); ---> campares the value (!)-


        // 3. "".length(); ---> counts number of characters


        // 4."".toUpperCase(); --> Upper Case
        //   ""..toLowerCase(); --. Lower Case


        //


        // 6. "".startWith(""); will check if STARTS or not with exact match - returns boolean

        // 7. "".endsWith(""); will check if ENDS or not with exact match - returns boolean

        // #8 - "".contains(""); --> will check if ("") anywhere in the String. If yes, returns TRUE, if not FALSE

        // #9 = "".trim(); --> remove space (only remove space from edges only)

        // #10 = "".isEmpty --> checks if String is empty and returns boolean

        // #11 = "".isEmpty --> check is String only has empty spaces and returns boolean

        // #12 = "".charAt() --> In the parantesis you can put number to of index number on word and returns CHARCTER

        // #13 = "".indexOf() --> you can put '' or  "" in parantesis and the result return NUMBER

        // #14 = "".lastIndexOf() start checking index from back

        // #15 "".substring(); ---> take the portion of the string and returns STRING

    }
}
